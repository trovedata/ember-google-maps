
import { average } from 'dummy/helpers/average';
import { module, test } from 'qunit';

module('Unit | Helper | average');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = average([42]);
  assert.ok(result);
});

